# .env.example - copia a .env y completa los valores antes de usar docker-compose

# Base de datos
DB_HOST=db
DB_PORT=3306
DB_USER=avnadmin
DB_PASSWORD=change_me_password
DB_NAME=gategroup_flights
MYSQL_ROOT_PASSWORD=example_root_pw

# Model (opcional): URL pública o privada desde donde descargar el joblib
# Si no usas descarga remota, deja vacio y asegúrate de tener los joblib localmente.
MODEL_URL=
